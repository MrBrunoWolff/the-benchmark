[install]
# Refuse to install npm versions published less than 3 days ago.
# Defence against supply-chain attacks. (bench.mjs itself has no dependencies —
# this guards the dev tooling and anything added later.)
minimumReleaseAge = 259200
